package bot.geography

/*
import bwem.{Area, BWMap}

case class Zone(name: String, area: Area)

object ZoneBuilder {
  def zones(map: BWMap): List[Zone] = {
    val area: Area = ???
    // map.getAreas.get(0)
  }
}
 */
